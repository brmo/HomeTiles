# Shared partition layout for the common B4/Tab5 project.
# Goal: large single factory app because the firmware is too large for the stock
# 16MB ESP32-P4 Arduino partition schemes and this project stores runtime data
# primarily on SD/NVS instead of an internal filesystem.
#
# Name,   Type, SubType, Offset,   Size,      Flags
nvs,      data, nvs,     0x9000,   0x5000,
app0,     app,  factory, 0x10000,  0xC00000,
ffat,     data, fat,     0xC10000, 0x3E0000,
coredump, data, coredump,0xFF0000, 0x10000,
